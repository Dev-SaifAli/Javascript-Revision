const array = [1, 2, 3, 4, 5];
console.log(array);
console.log(typeof array);

const arr = {
  0: 1,
  1: 2,
  2: 3,
  3: 4,
  4: 5,
};
/* An object in JS is a data type/structure used to store collections of related data and functionality. 
A container for properties:
key-value pairs
methods.
*/

console.log(arr);
console.log(Array.isArray(arr));

// isArray is a method to check if an object is an array - returns results as true / false.

