// "use strict" is a special instruction you can put at the top of your JavaScript code (or inside a function) to make the code run in Strict Mode.

"use strict";

function sum(a, b) {
  add = a + b;
  console.log(add);
}
sum(10, 10);
